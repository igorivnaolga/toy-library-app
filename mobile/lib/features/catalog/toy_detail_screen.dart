/// Toy detail page (photos, description, availability, actions).
///
/// Expected flow:
/// - loads a single toy from `GET /api/v1/toys/{toy_id}`
/// - member/admin actions (reserve, report issue) appear based on role later
class ToyDetailScreen {}
